name: simran world workflow
on:hi
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  hi:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echo "hello world"
        shell: bash

  bye:
    runs-on: ubuntu-latest
    steps:
      - name: goodbye world
        run: echo "goodbye world"
        shell: bash
        
